<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- File appender for local.log -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.ai-agent/local.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.ai-agent/local.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="FILE" />
    </root>
    
    <logger name="dev.local.ai" level="DEBUG" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

    <!-- JavaFX logging -->
    <logger name="javafx" level="DEBUG" additivity="false">
        <appender-ref ref="FILE" />
    </logger>
</configuration> 